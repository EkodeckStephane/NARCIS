
Authentication also has several multimedia meanings. FaceSigns~\cite{neekhara2024facesigns} embeds semi-fragile authentication information into media. \method keeps indexed covers unchanged and authenticates the protected payload and session state, providing a complementary carrier-preserving authentication model. Table~\ref{tab:positioning} summarizes the resulting methodological positioning while keeping each approach in its native recovery semantics.

\begin{table}[!htbp]
\caption{Positioning of representative recent approaches. Quantitative regime entries retain their native units, while the recovery-event column makes the success semantics explicit before numerical interpretation.}
\label{tab:positioning}
\footnotesize
\begin{tabularx}{\textwidth}{p{1.7cm}p{2.6cm}Y Y Y}
\toprule
Approach & Carrier / information object & Native quantitative regime & Recovery event & Evaluation emphasis\\
\midrule
Guo--Ping--Xu~\cite{guo2025active} & Selected natural images / hash sequence & VOC and COCO native-regime capacity/robustness evaluation & Native hash/retrieval robustness under passive and active steganalysis & Diversity, inverse retrieval, active/passive robustness\\
Guo--Ping~\cite{guo2026capacity} & Selected natural images / symbolic code & Reported 10-, 14-, and 15-bit nominal-capacity settings on Holidays, VOC, and ImageNet & Robustness evaluated with the source paper's native recovery metric & Stability-aware quantization, representative selection, robust nominal capacity\\
Meng et al.~\cite{meng2025restoration} & Existing coverless schemes & Restoration-based evaluation over geometric perturbations & Recovery of the underlying coverless decoder after restoration & Geometric robustness\\
DiffStega~\cite{yang2024diffstega} & Generated images / secret image & UniStega secret-image reconstruction regime & Secret-image reconstruction under the generative protocol & Perceptual recovery, password sensitivity, steganalysis\\
Li et al.~\cite{li2026mllm} & Generated images / semantic feature mapping & Multi-generator MLLM-assisted regime & Native semantic-feature recovery under the source protocol & Universality, lightweight deployment, diversity and robustness\\
\method & Unchanged indexed natural images / authenticated byte string & $K=8$; five covers/coded symbol; 8/32/64-byte plaintexts; 1,200 external holdout trials & Exact authenticated whole-plaintext recovery after majority decoding, RS correction, and AES-GCM verification & Finite-index feasibility, authenticated recovery, traffic cost, selection detectability\\
\bottomrule
\end{tabularx}
\end{table}

\subsection{Steganalysis of selection policies}
Selection steganalysis tests whether the emitted subset differs statistically from the available carrier population. CNN-based image steganalysis can exploit subtle distributional differences~\cite{li2023siamese}; adversarial embedding work likewise shows that the evaluated steganalyst materially shapes the security conclusion~\cite{wang2024adversarial}. Cao, Wang, and Zhang analyze cover-selection steganography directly and show that texture statistics can expose selection bias~\cite{cao2025selection}. \method therefore evaluates the selected-vs.-remainder distinction with residual, texture, and learned detectors under image-disjoint train/test splits.

\section{System and Threat Model}
\subsection{Parties and shared state}
Sender and receiver share an indexed image corpus $\mathcal{I}=\{x_i\}_{i=1}^{N}$, the trained descriptor and frozen codebook, and independent cryptographic subkeys for payload encryption, cluster mapping, and cover selection. The sender transmits existing indexed images unchanged. Session metadata records the authenticated sequence number, codebook size, padding information, and cover count. The implementation seals metadata with AES-GCM and binds it to sender/receiver identities.

The benchmark uses deterministic public key/nonce derivation to make every run reproducible. A production deployment can replace this benchmark root with an operational key-management service while preserving protocol interfaces. For a run-specific master key $K_{\mathrm{master}}$, HMAC-SHA256 derives subkeys for encryption, mapping, and selection. The benchmark nonce combines a four-byte HMAC-derived prefix with the unique 64-bit session sequence, giving explicit nonce uniqueness for sequences $0,\ldots,29$ under a fixed benchmark key.

\subsection{Channel and warden}
The image channel applies lossy compression, additive Gaussian noise, blur, resizing, central cropping, and small rotations. Calibration and holdout transformation values are disjoint. The receiver decodes directly from the received image stream under the declared channel family.

The evaluation distinguishes image-content observability from session-level traffic observability. For the image-content audit, a matched-index warden treats images selected by an authenticated session as the positive class and the remaining images in the same frozen index as the negative class; training and test image identities are disjoint. At the transport layer, an observer can also count transmitted images and observe timing. Because the implemented RS128 sessions use 2,320, 2,640, and 3,070 images for 8-, 32-, and 64-byte plaintexts, respectively, raw session volume carries payload-length-class information unless deployment traffic shaping is applied.

\subsection{Security goals and observation layers}
\method separates cryptographic protection, empirical carrier-selection security, and observable session traffic. AES-GCM provides payload confidentiality/integrity and metadata authentication under its standard AEAD assumptions and nonce discipline~\cite{dworkin2007gcm}; the replay guard rejects sequence numbers at or below the highest accepted value for a sender. Reed--Solomon coding~\cite{reed1960codes} corrects residual byte errors within its decoding budget. The detector battery quantifies image-content selection discrimination, while the measured image counts quantify the session-volume side channel. Fixed-volume batching, cover-count padding, dummy-cover insertion, and timing shaping provide deployment mechanisms for reducing length-class exposure.

\section{ARCIS Protocol}
Figure~\ref{fig:pipeline} summarizes the offline index-construction path and the online authenticated signaling path.
\begin{figure}[!htbp]
\centering
\begin{tikzpicture}[node distance=6mm and 7mm, every node/.style={font=\scriptsize,align=center}, box/.style={draw,rounded corners,minimum width=2.55cm,minimum height=8mm,fill=gray!5}, arr/.style={-Latex,thick}]
\node[box] (enc) {Learn normalized\\image descriptor};
\node[box,right=of enc] (code) {Calibration-locked\\projection + $K=8$ quantiles};
\node[box,right=of code] (bank) {Five-cover group bank\\+ failure signatures};
\node[box,below=10mm of enc] (auth) {AES-GCM + framing\\+ RS128};
\node[box,right=of auth] (map) {Keyed cyclic map\\Gray-indexed order};
\node[box,right=of map] (sel) {Signature-balanced\\keyed group selection};
\node[box,below=10mm of sel] (chan) {Image channel};
\node[box,left=of chan] (maj) {Majority labels\\+ RS decode};
\node[box,left=of maj] (plain) {Authenticated\\plaintext decision};
\draw[arr] (enc)--(code); \draw[arr] (code)--(bank);
\draw[arr] (auth)--(map); \draw[arr] (map)--(sel); \draw[arr] (bank)--(sel);
\draw[arr] (sel)--(chan); \draw[arr] (chan)--(maj); \draw[arr] (maj)--(plain);
\end{tikzpicture}
\caption{\method separates offline channel-aware index construction (top) from online protected transmission and recovery (bottom). The external-validation operating point is frozen before Caltech-101 outcomes are inspected.}
\Description{Flow diagram showing descriptor learning, a calibration-locked K=8 codebook, five-cover group-bank construction, authenticated Reed-Solomon framing, a keyed cyclic map with Gray-indexed symbol order, signature-balanced cover-group selection, the image channel, majority decoding, and authenticated plaintext acceptance.}
\label{fig:pipeline}
\end{figure}

\subsection{Invariant descriptor}
A convolutional encoder $f_\theta$ maps an image to an $L_2$-normalized descriptor $z=f_\theta(x)\in\mathbb{R}^{64}$. The frozen encoder contains four convolutional blocks with batch normalization and SiLU activations, adaptive average pooling, and a two-layer projector. Training uses paired channel augmentations and variance--invariance--covariance regularization in the spirit of VICReg~\cite{bardes2022vicreg}. In the external campaign, images are fit to $256\times256$ RGB at the channel boundary and $128\times128$ at the encoder boundary; base width is 16, batch size 16, and training lasts five epochs with AdamW (learning rate $10^{-3}$ and weight decay $10^{-5}$).

\subsection{Calibration-locked codebook and qualification}
For a candidate unit direction $u$, each indexed image receives scalar score
\begin{equation}
s_i=u^\top f_\theta(x_i).
\end{equation}
A balanced one-dimensional quantile codebook partitions the clean scores into $K=8$ labels. Candidate projections comprise the first 16 clean-descriptor principal directions and 32 deterministic random directions. Projection selection uses twelve calibration transformations: JPEG qualities 80 and 50; Gaussian noise standard deviations 5 and 12; Gaussian blur radii 0.8 and 1.5; resize scales 0.75 and 0.50; central crops 5\% and 10\%; and rotations 3 and 7 degrees.

Let $y_i$ be the clean label and $y_{i,a}$ the label under calibration attack $a$. Define correctness
\begin{equation}
c_{i,a}=\mathbb{1}[y_{i,a}=y_i].
\end{equation}
Projection candidates are ranked by analytical lower bounds on unavoidable majority-failing groups, followed by stable-cover and distribution-balance diagnostics. Holdout transformations, semantic category labels, detector outcomes, and final plaintext-recovery outcomes remain reserved for validation.

\subsection{Complementary group bank}
Within each codebook label, all covers of the 7,000-image index carrying that label are partitioned into disjoint groups of five. A group $G$ fails calibration attack $a$ when fewer than three members retain their clean label:
\begin{equation}
F_a(G)=\mathbb{1}\!\left[\sum_{i\in G} c_{i,a}<3\right].
\end{equation}
The vector $(F_a(G))_a$ is stored as a calibration majority-failure signature. Group-bank construction minimizes majority-failing group/attack cells using deterministic restarts and swap proposals and compares the achieved count against an analytical lower bound. Covers with complementary individual failure modes can therefore form a majority-correct communication unit.

During emission, groups of a requested label are partitioned by failure signature. If $p_s$ is the natural proportion of signature $s$ in the complete label-specific bank, the scheduler chooses each next available signature by its current proportional deficit. Secret-dependent HMAC ranking supplies deterministic ordering and tie breaking inside that rule. The emitted prefix consequently tracks the complete bank's signature mixture while preserving keyed group choice.

\subsection{Authenticated coding and exact session balancing}
The plaintext is encrypted with AES-GCM using associated data that include the authenticated sequence number. The protected envelope is framed and encoded with 128 Reed--Solomon parity bytes, then divided into $b=\log_2K=3$-bit symbols.

Let $\sigma$ be a fixed keyed bijection of the $K$ coded symbols onto positions $\{0,\ldots,K-1\}$. The implementation uses a Gray-indexed order: if $g(p)=p\oplus(p\gg1)$ is the binary-reflected Gray sequence, then $\sigma(g(p))=p$. A mapping subkey fixes a secret base $b_0\in\{0,\ldots,K-1\}$ and an orientation $r\in\{-1,+1\}$. For authenticated session sequence $q$,
\begin{equation}
\pi_q(i)=\bigl(b_0+q+r\,\sigma(i)\bigr)\bmod K.
\end{equation}
For any fixed symbol $i$ and any starting sequence $q_0$, the $K$ values $\pi_{q_0+t}(i)$, $0\le t<K$, are all distinct modulo $K$ and therefore cover every visual cluster exactly once. The exact balancing invariant follows from the cyclic shift in $q$; the Gray-indexed bijection specifies the implementation-level symbol ordering. Figure~\ref{fig:cycle} illustrates one normalized $K=8$ cycle. For each required mapped label, the sender chooses one five-cover group using the signature-balanced keyed scheduler and transmits the five unchanged images. Each cover appears at most once inside one transmission. The receiver re-embeds each image, recovers five labels, takes their strict majority, inverts $\pi_q$, reconstructs the coded bitstream, applies Reed--Solomon decoding, and accepts plaintext only after framing and AES-GCM verification.

\begin{figure}[!htbp]
\centering
